apiVersion: v1
kind: Service
metadata:
  name: kronos-kafka
  labels:
    {{- include "kronos.labels" . | nindent 4 }}
spec:
  selector:
    app: kronos-kafka
  ports:
    - name: broker
      port: 9092
      targetPort: 9092
    - name: controller
      port: 9093
      targetPort: 9093